<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_player_id %><br />
    <%= f.number_field :current_player_id %>
  </div>
  <div class="field">
    <%= f.label :board %><br />
    <%= f.text_field :board %>
  </div>
  <div class="field">
    <%= f.label :userwin %><br />
    <%= f.text_field :userwin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
